{
  "name": "erp-front",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start:dev": "cross-env APP_ENV=dev npm run start",
    "start:hlg": "cross-env APP_ENV=hlg npm run start",
    "start:prod": "cross-env APP_ENV=prod npm run start",
    "start": "echo Starting in $APP_ENV mode",
    "build:dev": "cross-env APP_ENV=dev npm run build",
    "build:hlg": "cross-env APP_ENV=hlg npm run build",
    "build:prod": "cross-env APP_ENV=prod npm run build",
    "build": "echo Building in $APP_ENV mode",
    "test": "jest --coverage"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.2",
    "sql.js": "^1.8.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.5",
    "@testing-library/react": "^14.2.1",
    "@testing-library/user-event": "^14.4.3",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.30",
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "cross-env": "^7.0.3"
  }
}
